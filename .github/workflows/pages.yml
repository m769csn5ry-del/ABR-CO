# Publie les applications du dépôt sur GitHub Pages.
#
# Deux applications cohabitent sur le même site :
#   /            Orga        (dossier todo/)
#   /lifeos/     LifeOS      (dossier lifeos/)
#
# Pages est servi depuis la branche `gh-pages` (c'est sa création qui a activé
# Pages : un workflow n'a pas le droit de créer le site lui-même — le jeton
# d'Actions se voit refuser l'opération). Ce workflow synchronise donc `todo/`
# vers `gh-pages` à chaque modification, puis attend et vérifie la mise en ligne.
#
# Le contenu de `todo/` est placé à la RACINE de `gh-pages` : l'app est servie sur
# /ABR-CO/ et non /ABR-CO/todo/, ce qui garde la portée du service worker et les
# chemins du manifeste corrects.

name: Publier les apps

on:
  push:
    branches: [main]
    paths:
      - 'todo/**'
      - 'lifeos/**'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  publier:
    name: Synchroniser gh-pages
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      sha: ${{ steps.sync.outputs.sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Vérifier que le paquet est complet
        run: |
          set -euo pipefail
          for f in todo/index.html todo/manifest.webmanifest todo/sw.js \
                   todo/icons/icon-180.png todo/icons/icon-192.png \
                   todo/icons/icon-512.png todo/icons/icon-maskable-512.png; do
            [ -f "$f" ] || { echo "::error::fichier manquant : $f"; exit 1; }
          done
          grep -q 'rel="manifest"' todo/index.html \
            || { echo "::error::le manifeste n'est pas lié dans index.html"; exit 1; }
          grep -q 'data:font/woff2;base64' todo/index.html \
            || { echo "::error::la police display n'est pas encastrée"; exit 1; }
          echo "Orga : $(du -sh todo | cut -f1)"

          for f in lifeos/index.html lifeos/manifest.webmanifest lifeos/sw.js \
                   lifeos/src/app.js lifeos/styles/tokens.css \
                   lifeos/icons/icon-180.png lifeos/icons/icon-512.png; do
            [ -f "$f" ] || { echo "::error::fichier manquant : $f"; exit 1; }
          done
          # Chaque script listé dans la page doit exister, et chacun doit être
          # mis en cache par le service worker : sinon l'app casse hors ligne.
          manquants=0
          for src in $(grep -o 'src="\./src/[^"]*"' lifeos/index.html | sed 's/src="\.\///;s/"//'); do
            [ -f "lifeos/$src" ] || { echo "::error::script introuvable : lifeos/$src"; manquants=1; }
            grep -q "$src" lifeos/sw.js || { echo "::error::script absent du cache hors ligne : $src"; manquants=1; }
          done
          [ "$manquants" = "0" ] || exit 1
          echo "LifeOS : $(du -sh lifeos | cut -f1)"

      - name: Publier sur gh-pages
        id: sync
        run: |
          set -euo pipefail
          sha="${GITHUB_SHA::7}"
          echo "sha=$sha" >> "$GITHUB_OUTPUT"

          git config user.name  'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'

          git fetch origin gh-pages --depth=1
          git worktree add ../site origin/gh-pages --detach
          git -C ../site checkout -B gh-pages

          # `--delete` : un fichier retiré de todo/ doit disparaître du site.
          # `lifeos` est exclu ici : ce dossier est synchronisé juste après.
          rsync -a --delete --exclude '.git' --exclude 'README.md' \
                --exclude 'lifeos' --exclude 'lifeos/**' todo/ ../site/

          # LifeOS vit dans son propre sous-dossier, servi tel quel.
          mkdir -p ../site/lifeos
          rsync -a --delete --exclude 'README.md' lifeos/ ../site/lifeos/
          touch ../site/.nojekyll
          # sert de témoin : la vérification attend de voir CE commit en ligne.
          echo "$sha" > ../site/version.txt

          cd ../site
          git add -A
          if git diff --cached --quiet; then
            echo "Rien de neuf à publier."
            exit 0
          fi
          git commit -qm "Publie $sha"
          git push origin gh-pages

  verifier:
    name: Vérifier le site en ligne
    needs: publier
    runs-on: ubuntu-latest
    permissions:
      pages: read
    steps:
      - name: Attendre puis contrôler l'app publiée
        env:
          GH_TOKEN: ${{ github.token }}
          ATTENDU: ${{ needs.publier.outputs.sha }}
        run: |
          set -uo pipefail

          base=$(curl -sS -H "Authorization: Bearer $GH_TOKEN" \
                   -H "Accept: application/vnd.github+json" \
                   "https://api.github.com/repos/${GITHUB_REPOSITORY}/pages" \
                 | jq -r '.html_url // ""')
          base="${base%/}"
          [ -n "$base" ] || { echo "::error::Pages inactif"; exit 1; }
          echo "Base : $base"

          # La construction Pages suit le push avec quelques dizaines de secondes
          # de retard : on attend de voir le témoin du commit courant.
          for i in $(seq 1 30); do
            vu=$(curl -sSL --max-time 20 "$base/version.txt" 2>/dev/null | tr -d '[:space:]' || true)
            [ "$vu" = "$ATTENDU" ] && { echo "Version en ligne : $vu (tentative $i)"; break; }
            echo "  … en ligne : '${vu:-rien}', attendu '$ATTENDU' (tentative $i)"
            sleep 10
          done
          [ "$vu" = "$ATTENDU" ] || { echo "::error::le site n'a pas repris le commit $ATTENDU"; exit 1; }

          fail=0
          check () {
            code=$(curl -sSL -o /dev/null -w '%{http_code}' --max-time 30 "$1" || echo 000)
            if [ "$code" = "$2" ]; then echo "  OK   $3 ($code)"
            else echo "::error::$3 — attendu $2, reçu $code ($1)"; fail=1; fi
          }
          check "$base/"                     200 "Orga — page d'accueil"
          check "$base/manifest.webmanifest" 200 "Orga — manifeste"
          check "$base/sw.js"                200 "Orga — service worker"
          check "$base/icons/icon-180.png"   200 "Orga — icône 180"
          check "$base/icons/icon-512.png"   200 "Orga — icône 512"

          check "$base/lifeos/"                     200 "LifeOS — page d'accueil"
          check "$base/lifeos/manifest.webmanifest" 200 "LifeOS — manifeste"
          check "$base/lifeos/sw.js"                200 "LifeOS — service worker"
          check "$base/lifeos/src/app.js"           200 "LifeOS — assemblage"
          check "$base/lifeos/styles/tokens.css"    200 "LifeOS — jetons de design"
          check "$base/lifeos/icons/icon-180.png"   200 "LifeOS — icône 180"

          html=$(curl -sSL --max-time 30 "$base/")
          for m in 'rel="manifest"' 'apple-mobile-web-app-capable' 'Bodoni Moda' 'data-tab="today"'; do
            case "$html" in
              *"$m"*) echo "  OK   marqueur présent : $m" ;;
              *) echo "::error::marqueur absent de la page servie : $m"; fail=1 ;;
            esac
          done
          case "$html" in
            *fonts.googleapis.com*|*fonts.gstatic.com*)
              echo "::error::appel à un hébergeur de polices : l'app ne serait plus autonome hors connexion"; fail=1 ;;
            *) echo "  OK   police encastrée, aucun appel externe" ;;
          esac

          life=$(curl -sSL --max-time 30 "$base/lifeos/")
          for m in 'rel="manifest"' 'apple-mobile-web-app-capable' 'src/app.js' 'id="app"'; do
            case "$life" in
              *"$m"*) echo "  OK   LifeOS — marqueur présent : $m" ;;
              *) echo "::error::LifeOS — marqueur absent de la page servie : $m"; fail=1 ;;
            esac
          done

          [ "$fail" = "0" ] || { echo "::error::vérification en ligne en échec"; exit 1; }
          { echo "## Les apps répondent en ligne"; echo ""
            echo "- Orga : **$base/**"
            echo "- LifeOS : **$base/lifeos/**"
            echo ""
            echo "Commit \`$ATTENDU\` — pages, manifestes, service workers et icônes vérifiés."
          } >> "$GITHUB_STEP_SUMMARY"
