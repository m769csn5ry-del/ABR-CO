{
  "name": "abr-co",
  "version": "1.0.0",
  "private": true,
  "description": "Orga (agenda de tâches DBR Studio) + expériences web ABR-CO.",
  "scripts": {
    "build": "node build.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
